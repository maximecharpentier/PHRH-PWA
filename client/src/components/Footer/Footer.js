import React from 'react';
import { StyledFooter, StyledFooterTitle } from './StyledFooter';

const Footer = () => (
    <StyledFooter>
      <StyledFooterTitle>
        © 2020
      </StyledFooterTitle>
    </StyledFooter>
);

export default Footer;
